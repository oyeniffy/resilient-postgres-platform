# Terraform
**/.terraform/*
*.tfstate
*.tfstate.*
crash.log
crash.*.log
*.tfvars
*.tfvars.json
override.tf
override.tf.json
*_override.tf
*_override.tf.json
.terraformrc
terraform.rc
# NOTE: .terraform.lock.hcl is intentionally NOT ignored — it should be
# committed to pin provider versions for reproducible builds.

# Keep example tfvars
!*.tfvars.example

# Secrets / credentials
*.pem
*.key
.env
.env.*
service-principal.json

# OS / editor
.DS_Store
.vscode/
*.swp

# Logs
*.log
budget-notifications.json
app/*.zip
