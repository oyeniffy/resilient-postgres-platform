# South Africa West (secondary/passive) — root configuration
#
# Intentionally empty for now. Modules (networking, database, compute)
# will be wired in here once each module is built and reviewed.
# Kept empty rather than stubbed so `terraform init` / `terraform plan`
# can be validated against the backend before any real resources exist.
